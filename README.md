# Unity-Mouse-Polling-Rate-Fix
Attachable script to fix high polling rates (above 1000hz) causing stuttering in Unity (both editor and build)

# How to use
Attach this to a new empty game object that your game starts with, and voilà.
